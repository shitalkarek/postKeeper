<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/public/stylesheets/style.css">
    
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="p-10 w-full min-h-screen bg-zinc-900 text-white">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout</a>
        </div>
        
        

        <h5 class="mb-5"> Edit your post.</h5>
        
        <form method="post" action="/update/<%= post._id  %>">
            <textarea placeholder="What's on your mind" class="w-1/3 block p-3 outline-none resize-none bg-transparent border-2 border-zinx-800 rounded-md" name="content">
                <%= post.content  %>
            </textarea>
            <input class="w-40 px-3 py-2 bg-yellow-500 block text-sm rounded-md mt-2" type="submit" value="update Post">
        </form>

       
    </div>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/public/stylesheets/style.css">
    <!-- Importing Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
</head>
<body class="bg-gradient-to-r from-gray-100 to-gray-300 min-h-screen flex items-center justify-center p-4">
    <div class="bg-white rounded-lg shadow-lg overflow-hidden w-full max-w-2xl">
        <!-- Navigation Bar -->
        <div class="flex justify-between items-center bg-gradient-to-r from-blue-500 to-indigo-600 text-white p-4">
            <h1 class="text-2xl font-bold">PostKeeper</h1>
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block hover:bg-red-600 transition duration-300" href="/logout">Logout</a>
        </div>

        <!-- Edit Post Form -->
        <div class="p-8">
            <h3 class="text-3xl font-bold gradient-text mb-4">Edit Your Post</h3>
            <form method="post" action="/update/<%= post._id %>">
                <textarea placeholder="What's on your mind?" class="w-full block p-3 outline-none resize-none bg-gray-100 border-2 border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-300" name="content"><%= post.content %></textarea>
                <input class="mt-4 px-4 py-2 bg-gradient-to-r from-yellow-500 to-orange-500 text-white font-semibold rounded-lg shadow-md cursor-pointer hover:bg-gradient-to-l hover:from-yellow-600 hover:to-orange-600 transition duration-300" type="submit" value="Update Post">
            </form>
        </div>
    </div>
</body>
</html>
